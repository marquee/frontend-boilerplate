{
  "private": true,
  "main": "./main.cjsx",
  "scripts": {
    "build": "echo 'Needs setup - run `npm run setup`'",
    "develop": "echo 'Needs setup - run `npm run setup`'",
    "deploy": "echo 'Needs setup - run `npm run setup`'",
    "setup": "npm install && ./node_modules/.bin/marqueestatic setup"
  },
  "marquee": "RUN `npm run setup` FIRST",
  "dependencies": {
    "marquee-static-sdk": "^0.7.0-alpha.3",
    "react": "^15.0.1",
    "shiny": "^1.0.0",
    "when": "^3.7.3"
  }
}